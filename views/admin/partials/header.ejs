<nav class="admin-nav">
    <button id="nav-toggle" class="nav-toggle">☰</button>
    <h1><%= title %></h1>
    <div class="nav-right">
        <div class="user-info">
            <span class="username"><%= currentUser.name %> | <%= currentUser.email %></span>
            <img src="<%= currentUser?.avatar?.path ? `${server_url}/${currentUser?.avatar?.path}` : '/assets/images/default-avatar.jpg' %>" alt="Avatar" class="avatar">
        </div>
    </div>
</nav>
